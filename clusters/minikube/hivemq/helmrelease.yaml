apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hivemq
  namespace: hivemq
spec:
  interval: 1m
  chart:
    spec:
      chart: ./charts/hivemq
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 1m
